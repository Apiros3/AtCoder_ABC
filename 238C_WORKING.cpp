#include <iostream>
#include <cmath>
#include <algorithm>
#include <numeric>
#include <iomanip>
#include <vector>
#include <algorithm>
using namespace std;


int main()
{
    long long int N, mod=998244353;
    cin >> N;

    long long int num=N, ans=0;

    for (int i=0; i<18; i++) {
        long long int K=pow(10,i)-1;
        if ()
        int temp=min(K,N)-pow(10,i-1)
        ans+=((min(K,N)-pow(10,))%mod*(min(K,N)+1)%mod*mod/2)%mod;
    }



    return 0;
}     